syntax = "proto2";
package openscenairo;

// Enum for Role
enum Role {
    NONE = 0;
    AMBULANCE = 1;
    CIVIL = 2;
    FIRE = 3;
    MILITARY = 4;
    POLICE = 5;
    PUBLIC_TRANSPORT = 6;
    ROAD_ASSISTANCE = 7;
}